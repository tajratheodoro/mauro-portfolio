# Mauro Junior Portfolio

Portfólio web para Mauro Junior, filmmaker e fotógrafo.

## Rodar localmente

**Pré-requisito:** Node.js

1. Instale as dependências:
   `npm install`
2. Rode o projeto:
   `npm run dev`
3. Gere o build de produção:
   `npm run build`
